{% extends 'backoffice/admin/Partials/container.html.twig' %}

{% block title %}
  {% if event is defined %}
    Inscriptions - {{ event.title }}
  {% else %}
    Inscriptions - Événements
  {% endif %}
{% endblock %}

{% block content %}
<div class="container mx-auto px-4 py-8">

  {# HEADER #}
  <div class="px-8 py-10 bg-gradient-to-r from-blue-500 to-indigo-600 text-white rounded-t-2xl">
    <div class="flex flex-col lg:flex-row justify-between items-start lg:items-center gap-6">
      <div>
        {% if event is defined %}
          <h1 class="text-4xl font-bold mb-2">🧾 Inscriptions</h1>
          <p class="text-blue-100">Événement : <b>{{ event.title }}</b></p>
        {% else %}
          <h1 class="text-4xl font-bold mb-2">🧾 Inscriptions par événement</h1>
          <p class="text-blue-100">Choisis un événement pour voir les inscrits.</p>
        {% endif %}
      </div>

      <div class="flex flex-wrap gap-3">
        {% if event is defined %}
          <a href="{{ path('admin_event_registrations_events') }}"
             class="inline-flex items-center px-6 py-3 bg-white/20 backdrop-blur-sm text-white font-semibold rounded-xl hover:bg-white/30 transition-all duration-300 border border-white/30">
            <span class="material-symbols-outlined mr-2 text-base">arrow_back</span>
            Tous les événements
          </a>
        {% endif %}

        <a href="{{ path('admin_event_index') }}"
           class="inline-flex items-center px-6 py-3 bg-white text-indigo-700 font-extrabold rounded-xl hover:bg-indigo-50 transition-all duration-300 shadow-sm">
          <span class="material-symbols-outlined mr-2 text-base">calendar_month</span>
          Événements
        </a>
      </div>
    </div>
  </div>

  <div class="bg-white rounded-b-2xl shadow-lg border border-gray-100 p-6">

    {# SEARCH + TRI (mode liste events seulement) #}
    <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4 mb-6">

      <div class="flex items-center gap-3">
        <div class="w-10 h-10 rounded-xl bg-indigo-50 flex items-center justify-center">
          <span class="material-symbols-outlined text-indigo-600">search</span>
        </div>
        <div>
          <div class="text-xl font-extrabold text-gray-900">
            {% if event is defined %}Rechercher dans les inscriptions{% else %}Rechercher un événement{% endif %}
          </div>
          <div class="text-sm text-gray-500 font-semibold">
            {% if event is defined %}Enfant / téléphone / contact{% else %}Titre / lieu{% endif %}
          </div>
        </div>
      </div>

      <div class="flex flex-col md:flex-row gap-3 w-full lg:w-auto">

        {# ✅ champ recherche (serveur) + dynamique #}
        <form method="get" class="relative w-full md:w-96">
          <span class="material-symbols-outlined absolute left-3 top-2.5 text-gray-400 text-sm">search</span>
          <input
            id="liveSearch"
            name="q"
            value="{{ q|default('') }}"
            class="w-full pl-10 pr-4 py-2.5 border border-gray-200 rounded-xl text-sm focus:ring-2 focus:ring-blue-500 focus:outline-none"
            placeholder="{% if event is defined %}Rechercher (enfant, tel...){% else %}Rechercher (titre, lieu...){% endif %}">
          {# garder tri si existant #}
          {% if sort is defined %}<input type="hidden" name="sort" value="{{ sort }}">{% endif %}
          {% if order is defined %}<input type="hidden" name="order" value="{{ order }}">{% endif %}
        </form>

        {# ✅ TRI : uniquement sur la liste des events #}
        {% if rows is defined %}
          {% set currentSort = sort|default('start') %}
          {% set currentOrder = order|default('desc') %}

          <div class="flex flex-wrap gap-2">
            {% set nextOrderStart = (currentSort == 'start' and currentOrder == 'asc') ? 'desc' : 'asc' %}
            {% set nextOrderEnd = (currentSort == 'end' and currentOrder == 'asc') ? 'desc' : 'asc' %}
            {% set nextOrderCount = (currentSort == 'count' and currentOrder == 'asc') ? 'desc' : 'asc' %}

            <a href="{{ path('admin_event_registrations_events', { q: q|default(''), sort: 'start', order: nextOrderStart }) }}"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-gray-200 bg-gray-50 hover:bg-gray-100 font-extrabold text-sm">
              <span class="material-symbols-outlined text-base">schedule</span>
              Début
              {% if currentSort == 'start' %}
                <span class="text-xs font-black text-indigo-700">({{ currentOrder|upper }})</span>
              {% endif %}
            </a>

            <a href="{{ path('admin_event_registrations_events', { q: q|default(''), sort: 'end', order: nextOrderEnd }) }}"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-gray-200 bg-gray-50 hover:bg-gray-100 font-extrabold text-sm">
              <span class="material-symbols-outlined text-base">event</span>
              Fin
              {% if currentSort == 'end' %}
                <span class="text-xs font-black text-indigo-700">({{ currentOrder|upper }})</span>
              {% endif %}
            </a>

            <a href="{{ path('admin_event_registrations_events', { q: q|default(''), sort: 'count', order: nextOrderCount }) }}"
               class="inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-gray-200 bg-gray-50 hover:bg-gray-100 font-extrabold text-sm">
              <span class="material-symbols-outlined text-base">groups</span>
              Inscriptions
              {% if currentSort == 'count' %}
                <span class="text-xs font-black text-indigo-700">({{ currentOrder|upper }})</span>
              {% endif %}
            </a>
          </div>
        {% endif %}

      </div>
    </div>

    {# =========================
       MODE A : LISTE EVENTS
       ========================= #}
    {% if rows is defined %}

      {% if rows is empty %}
        <div class="p-6 rounded-2xl border border-dashed border-gray-300 bg-gray-50">
          <p class="font-extrabold text-gray-700">Aucun événement.</p>
        </div>
      {% else %}
        <div class="overflow-hidden rounded-2xl border border-gray-200">
          <table class="w-full text-left">
            <thead class="bg-gradient-to-r from-gray-50 to-gray-100">
              <tr>
                <th class="p-4 font-extrabold text-gray-700">Titre</th>
                <th class="p-4 font-extrabold text-gray-700">Dates</th>
                <th class="p-4 font-extrabold text-gray-700">Lieu</th>
                <th class="p-4 font-extrabold text-gray-700">Inscriptions</th>
                <th class="p-4 font-extrabold text-gray-700">Action</th>
              </tr>
            </thead>

            <tbody id="eventsRows" class="divide-y divide-gray-200">
              {% for row in rows %}
                {% set e = row.event %}
                <tr class="hover:bg-indigo-50/40 transition"
                    data-search="{{ (e.title ~ ' ' ~ (e.location ?: '') ~ ' ' ~ (e.startDate ? e.startDate|date('d/m/Y H:i') : '') ~ ' ' ~ (e.endDate ? e.endDate|date('d/m/Y H:i') : ''))|lower }}">
                  <td class="p-4 font-extrabold text-gray-900">{{ e.title }}</td>
                  <td class="p-4 font-semibold text-gray-700">
                    {{ e.startDate|date('d/m/Y H:i') }} → {{ e.endDate|date('d/m/Y H:i') }}
                  </td>
                  <td class="p-4 font-semibold text-gray-700">{{ e.location ?: '—' }}</td>
                  <td class="p-4">
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-extrabold bg-blue-50 text-blue-700 border border-blue-100">
                      {{ row.regCount }}
                    </span>
                  </td>
                  <td class="p-4">
                    <a href="{{ path('admin_event_registrations_show', {id: e.id}) }}"
                       class="inline-flex items-center px-4 py-2 rounded-xl bg-blue-600 text-white font-extrabold hover:bg-blue-700 transition">
                      <span class="material-symbols-outlined mr-2 text-base">group</span>
                      Voir
                    </a>
                  </td>
                </tr>
              {% endfor %}
            </tbody>

          </table>
        </div>

        <div id="noLiveResult" class="hidden mt-4 p-6 rounded-2xl border border-dashed border-gray-300 bg-gray-50">
          <p class="font-extrabold text-gray-700">Aucun résultat.</p>
        </div>
      {% endif %}

    {# =========================
       MODE B : INSCRIPTIONS EVENT
       ========================= #}
    {% elseif registrations is defined %}

      {% if registrations is empty %}
        <div class="p-6 rounded-2xl border border-dashed border-gray-300 bg-gray-50">
          <p class="font-extrabold text-gray-700">Aucune inscription pour cet événement.</p>
        </div>
      {% else %}
        <div class="overflow-hidden rounded-2xl border border-gray-200">
          <table class="w-full text-left">
            <thead class="bg-gradient-to-r from-gray-50 to-gray-100">
              <tr>
                <th class="p-4 font-extrabold text-gray-700">Enfant</th>
                <th class="p-4 font-extrabold text-gray-700">Parent</th>
                <th class="p-4 font-extrabold text-gray-700">Téléphone</th>
                <th class="p-4 font-extrabold text-gray-700">Classe</th>
                <th class="p-4 font-extrabold text-gray-700">Statut</th>
                <th class="p-4 font-extrabold text-gray-700">Inscrit le</th>
              </tr>
            </thead>

            <tbody class="divide-y divide-gray-200">
              {% for r in registrations %}
                {% set st = (r.status ?: 'PENDING')|upper %}
                {% set badge = 'bg-yellow-50 text-yellow-700 border border-yellow-200' %}
                {% if st == 'APPROVED' %}{% set badge = 'bg-green-50 text-green-700 border border-green-200' %}{% endif %}
                {% if st == 'CANCELLED' %}{% set badge = 'bg-red-50 text-red-700 border border-red-200' %}{% endif %}

                <tr class="hover:bg-indigo-50/40 transition">
                  <td class="p-4 font-extrabold text-gray-900">{{ r.childFullName ?: '—' }}</td>
                  <td class="p-4">
                    <div class="font-bold text-gray-900">
                      {% if r.parent %}{{ r.parent.firstName ~ ' ' ~ r.parent.lastName }}{% else %}—{% endif %}
                    </div>
                    <div class="text-sm text-gray-500 font-semibold">
                      {{ r.parent ? r.parent.email : '—' }}
                    </div>
                  </td>
                  <td class="p-4 font-bold text-gray-800">{{ r.parentPhone ?: '—' }}</td>
                  <td class="p-4">
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-extrabold bg-indigo-50 text-indigo-700 border border-indigo-100">
                      {{ r.childClassLevel ?: '—' }}
                    </span>
                  </td>
                  <td class="p-4">
                    <span class="inline-flex items-center px-3 py-1 rounded-full text-xs font-extrabold {{ badge }}">
                      {{ st }}
                    </span>
                  </td>
                  <td class="p-4 font-semibold text-gray-700">
                    {{ r.registeredAt ? r.registeredAt|date('d/m/Y H:i') : '—' }}
                  </td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      {% endif %}

    {% else %}
      <div class="p-6 rounded-2xl border border-dashed border-gray-300 bg-gray-50">
        <p class="font-extrabold text-gray-700">Aucune donnée à afficher.</p>
      </div>
    {% endif %}

  </div>
</div>

{# ✅ Recherche dynamique (mode liste events uniquement) #}
{% if rows is defined %}
<script>
(function () {
  const input = document.getElementById('liveSearch');
  const tbody = document.getElementById('eventsRows');
  const emptyBox = document.getElementById('noLiveResult');
  if (!input || !tbody) return;

  function refresh() {
    const q = (input.value || '').trim().toLowerCase();
    let visible = 0;

    tbody.querySelectorAll('tr').forEach(tr => {
      const hay = (tr.getAttribute('data-search') || '');
      const ok = q === '' || hay.includes(q);
      tr.style.display = ok ? '' : 'none';
      if (ok) visible++;
    });

    if (emptyBox) {
      emptyBox.classList.toggle('hidden', visible !== 0);
    }
  }

  input.addEventListener('input', refresh);
  refresh();
})();
</script>
{% endif %}
{% endblock %}