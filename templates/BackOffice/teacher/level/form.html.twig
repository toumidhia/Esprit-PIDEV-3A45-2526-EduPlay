{{ form_start(form) }}
  {{ form_row(form.name) }}
  {{ form_row(form.type) }}
  {{ form_row(form.description) }}
  {{ form_row(form.image) }}
  {{ form_row(form.idLevel) }}

  <button type="submit">Enregistrer</button>
{{ form_end(form) }}
