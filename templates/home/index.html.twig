{% extends 'base.html.twig' %}

{% block title %}{{ titre }}{% endblock %}

{% block content %}
    <div class="container">
        <h1>{{ titre }}</h1>
        <p>{{ description }}</p>
        
        {# Utilisez les classes CSS de votre template #}
        <div class="row">
            <div class="col-md-4">
                <div class="card">
                    <h3>Section 1</h3>
                    <p>Contenu dynamique</p>
                </div>
            </div>
        </div>
    </div>
{% endblock %}