<aside class="admin-sidebar w-64 bg-white border-r border-gray-200 flex flex-col h-screen">

    <div class="p-6 border-b border-gray-200">
        <div class="flex items-center gap-3">
            <div class="w-12 h-12 bg-gradient-to-br from-blue-500 to-blue-600 rounded-xl flex items-center justify-center">
                <span class="material-symbols-outlined text-white text-2xl">rocket_launch</span>
            </div>
            <div>
                <h2 class="text-xl font-bold text-gray-900">EduPlay Admin</h2>
                <p class="text-sm text-gray-600">Super Admin</p>
            </div>
        </div>
    </div>

    <nav class="sidebar-nav flex-1 p-4 space-y-2">
        <a href="#" class="active flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-50">
            Dashboard
        </a>

        <a href="#" class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-50">
            Library
        </a>

        <a href="{{ path('app_admin_users') }}" class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-50">
            Users
        </a>

        <a href="#" class="flex items-center gap-3 px-4 py-3 rounded-lg hover:bg-gray-50">
            Settings
        </a>
    </nav>

    <div class="p-4 border-t border-gray-200">
        <a href="{{ path('app_logout') }}" class="flex items-center gap-3 p-3 bg-gray-50 rounded-lg hover:bg-gray-100">
            Logout
        </a>
    </div>

</aside>
