{% extends 'base.html.twig' %}

{% block title %}Edit Session{% endblock %}

{% block body %}
<div class="container mx-auto px-6 py-8 max-w-4xl">
    <!-- Header -->
    <div class="mb-8">
        <h1 class="text-4xl font-extrabold text-[#111518] dark:text-white mb-2">Edit Session</h1>
        <p class="text-[#617989] dark:text-gray-400">
            Update session information
        </p>
    </div>

    <!-- Flash Messages -->
    {% for type, messages in app.flashes %}
        {% for message in messages %}
            <div class="mb-4 p-4 rounded-lg {% if type == 'success' %}bg-green-100 text-green-800{% elseif type == 'error' %}bg-red-100 text-red-800{% else %}bg-yellow-100 text-yellow-800{% endif %}">
                {{ message }}
            </div>
        {% endfor %}
    {% endfor %}

    <!-- Form Card -->
    <div class="bg-white dark:bg-gray-800 rounded-xl shadow-lg p-8">
        {{ include('seance/_form.html.twig', {'button_label': 'Update'}) }}
    </div>

    <!-- Delete Form -->
    <div class="mt-6">
        {{ include('seance/_delete_form.html.twig') }}
    </div>
</div>
{% endblock %}
